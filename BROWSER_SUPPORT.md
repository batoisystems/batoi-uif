# Browser Support

Batoi UIF targets modern evergreen browsers with native support for ES modules, CustomEvent, fetch, FormData, and CSS custom properties.

Baseline:

- Chrome, Edge, Firefox, and Safari current stable versions.
- Mobile Safari and Chrome for Android current stable versions.
- Progressive enhancement is required when APIs such as Service Workers, Push, Notifications, ResizeObserver, EventSource, or WebSocket are unavailable.
